import PlaceholderScreen from '../placeholder/PlaceholderScreen';

export default function AccountDetailScreen(props) {
  return <PlaceholderScreen {...props} title="Account Detail" description="Account insights, planning data, and activity history will live here." />;
}
