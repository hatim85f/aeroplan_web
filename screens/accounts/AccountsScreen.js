import PlaceholderScreen from '../placeholder/PlaceholderScreen';

export default function AccountsScreen(props) {
  return <PlaceholderScreen {...props} title="Accounts" description="Account list and search tools will be built here." />;
}
